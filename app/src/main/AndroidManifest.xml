<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="ekonsul.anasbayu.com.ekonsul">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".moduls.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".moduls.register.RegisterActivity">
        </activity>
        <activity android:name=".moduls.pengumuman.PengumumanActivity">
        </activity>
        <activity android:name=".moduls.daftardosen.DaftarDosenActivity">
        </activity>
        <activity android:name=".moduls.bookkonsultasi.BookActivity">
        </activity>
        <activity android:name=".moduls.pengumuman.DetailPengumuman.DetailPengumumanActivity">
        </activity>
    </application>

</manifest>